---
title: Hello World | Ajit Singh Kamal
author: Ajit Singh Kamal
---

import { graphql } from "gatsby";
import { Seo, Banner, Work, Social, Flex } from "../components";

<Seo title={props.pageContext.frontmatter.title}></Seo>
<Banner greeting="Hello!! world">
	<h1>I am Ajit</h1>
	<p>I am a UI/UX developer and I make cool stuff for web.</p>
</Banner>
<Social navData={Object.values(props.data.site.siteMetadata.social)} />
<Work portfolio={props.data.site.siteMetadata.work} />

export const pageQuery = graphql`
	query {
		site {
			siteMetadata {
				work {
					from
					to
					name
				}
				social {
					linkedin {
						url
						label
					}
					mail {
						url
						label
					}
					git {
						url
						label
					}
					codepen {
						url
						label
					}
					stackoverflow {
						url
						label
					}
				}
			}
		}
	}
`;
