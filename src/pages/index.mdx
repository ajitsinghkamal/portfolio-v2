---
title: Hello World | Ajit Singh Kamal
author: Ajit Singh Kamal
---

import { graphql } from "gatsby";
import { Seo, Banner, Work } from "../components";

<Seo title={props.pageContext.frontmatter.title}></Seo>
<Banner
	greeting="Hello Everyone!!"
	title={props.pageContext.frontmatter.author}
	links={[
		{ url: props.data.site.siteMetadata.emailLink, label: "Email", id: "mail" },
		{
			url: props.data.site.siteMetadata.linkedinLink,
			label: "LinkedIn",
			id: "linkedin",
		},
	]}
>
	A UI / UX developer based in India. I create rich and accessible user
	experiences for the web with a keep focus on the aesthetics, semantics, and
	scalability of the product.
</Banner>

<Work
	from="2019"
	name="UpGrad"
	shade={1}
	image={props.data.upgrad && props.data.upgrad.childImageSharp.fluid}
/>

<Work
	from="2018"
	to="2019"
	name="Cyware"
	shade={2}
	image={props.data.cyware && props.data.cyware.childImageSharp.fluid}
/>

export const pageQuery = graphql`
	query {
		site {
			siteMetadata {
				emailLink
				linkedinLink
			}
		}
		upgrad: file(relativePath: { eq: "work/upgrad.png" }) {
			childImageSharp {
				fluid {
					src
					srcSet
					base64
					aspectRatio
					originalImg
					sizes
				}
			}
		}
		cyware: file(relativePath: { eq: "work/cyware.png" }) {
			childImageSharp {
				fluid {
					src
					srcSet
					base64
					aspectRatio
					originalImg
					sizes
				}
			}
		}
	}
`;
