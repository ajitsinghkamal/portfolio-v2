---
title: Hello World | Ajit Singh Kamal
author: Ajit Singh Kamal
---

import { graphql } from "gatsby";
import { Banner, Work, Social } from "@components";

<Seo title={props.pageContext.frontmatter.title} />
<Banner greeting="Hello!! world">
	<h1>I am Ajit</h1>
	<p>I build stuff for the web.</p>
</Banner>
<Social navData={Object.values(props.data.site.siteMetadata.social)} />
<Work portfolio={props.data.site.siteMetadata.work} />

export const pageQuery = graphql`
	query {
		site {
			siteMetadata {
				work {
					from
					to
					name
					tech
					link
				}
				social {
					linkedin {
						url
						label
					}
					mail {
						url
						label
					}
					git {
						url
						label
					}
					codepen {
						url
						label
					}
					stackoverflow {
						url
						label
					}
				}
			}
		}
	}
`;
