import { css } from "@emotion/core";

export const CssFixed = css`
	top: 0;
	left: 0;
	position: fixed;
`;

export const CssSpacer = css({
	padding: "1.9em 0",
});

export const CssIcon = css({
	height: "18px",
	width: "18px",
	marginRight: "12px",
});
